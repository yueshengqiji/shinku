# NOTICE

## 本仓库是什么

Shinku 的**独立实现**（洁净室重建产物）。

## 当前声明（B1 阶段，截至 2026-09-18）

本仓库当前包含的**全部**内容是：

- `src/shinku/` 下的骨架代码（命名契约、配置加载、应用工厂、命令行入口、包的占位标记）；
- `tests/` 下针对上述骨架的测试；
- `docs/`、`README.md`、本文件、构建配置。

以上内容均为**本仓库原创**，不复制、不改写、不拼接 `AkaneCompanionLab` 的源码、注释、
目录组织或测试文本。

**本仓库不含**：Akane 的任何源码或测试；角色原作内容、游戏台词、美术、Live2D、声音或网络素材；
第三方二进制。第三方 Python 依赖见 `pyproject.toml`。

## 与旧项目的关系

`AkaneCompanionLab_Shinku`（旧项目）自本仓库建立之日起**降级为只读内部来源档案**：
它只用于来源追溯和对照，**不再是本仓库的开发基线**，也不被本仓库作为代码、测试或运行时依赖。

详见 `docs/SOURCE_RECORD.md`。

## 后续维护规则

C 阶段每接入一个重写完成的模块，**必须同时更新本文件**，写清：

- 该模块的来源（洁净室重写的批次号 / 行为契约文档）；
- 该模块依赖的第三方库及其许可证。

这条规则的意义：让"本仓库不含 Akane 代码"这句话在**任意时点**都可被逐文件核对，
而不是只在建仓那天成立。

## 许可

本仓库的发布许可尚未确定。`pyproject.toml` 里暂时标为 `Proprietary`，
表示"尚未授权公开发布"，而不是"选择闭源"。

许可选择、第三方许可证清单、素材授权边界均属于计划 D 阶段
（依赖、素材与发布边界审计）的范围，**在 D 阶段完成前不得对外发布本仓库**。
