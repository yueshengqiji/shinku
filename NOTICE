# NOTICE

## 本仓库是什么

Shinku 的**独立实现**（洁净室重建产物）。

## 当前声明（D1 发布边界审计，截至 2026-09-20）

本仓库当前包含的内容是：

- `src/shinku/` 下按行为契约独立重写的运行时代码，包括 LLM runtime、Agent、工具宿主、
  QQ/NapCat 适配、图片边界、记忆系统和健康探针；
- `scripts/` 下的独立记忆迁移、准入审计和发布检查脚本；
- `tests/` 下针对上述独立实现的契约与联合回归；
- `docs/`、`README.md`、本文件、构建配置和第三方许可证清单。

上述业务实现按 `docs/SOURCE_RECORD.md` 的记录独立创建或洁净室重写；不复制、不改写、
不拼接 `AkaneCompanionLab` 的源码、注释、目录组织或测试文本。来源台账和契约文档中
对旧项目的提及仅用于来源审计，不属于运行时依赖。

**本仓库不含**：Akane 的任何源码或测试；角色原作内容、游戏台词、美术、Live2D、声音或网络素材；
第三方二进制。第三方 Python 依赖见 `pyproject.toml`，对应许可证清单见
`docs/THIRD_PARTY_LICENSES.md`。本仓库不携带这些依赖的源码或二进制。

## 与旧项目的关系

`AkaneCompanionLab_Shinku`（旧项目）自本仓库建立之日起**降级为只读内部来源档案**：
它只用于来源追溯和对照，**不再是本仓库的开发基线**，也不被本仓库作为代码、测试或运行时依赖。

详见 `docs/SOURCE_RECORD.md`。

## 后续维护规则

C 阶段每接入一个重写完成的模块，**必须同时更新** `docs/SOURCE_RECORD.md` 和本文件
相关的依赖说明，写清：

- 该模块的来源（洁净室重写的批次号 / 行为契约文档）；
- 该模块依赖的第三方库及其许可证。

这条规则的意义：让“本仓库不含 Akane 代码”这句话在**任意时点**都可被逐文件核对，
而不是只在建仓那天成立。

## 许可

Copyright 2026 yueshengqiji

本仓库原创代码、文档和测试采用 Apache-2.0，完整文本见根目录 `LICENSE`。
第三方依赖不属于本仓库原创代码，仍须按 `docs/THIRD_PARTY_LICENSES.md` 和各自
许可证文本履行义务。角色原作内容、游戏台词、美术、Live2D、声音和网络素材不在
本仓库授权范围内。
